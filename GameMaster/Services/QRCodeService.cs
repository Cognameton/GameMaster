namespace GameMaster.Services;

/// <summary>
/// In the private app, this generates QR codes for player join links.
/// This showcase contains only a structural placeholder.
/// </summary>
public class QRCodeService
{
    public byte[] GenerateQrCode(string text)
    {
        throw new System.NotImplementedException("QR generation is private.");
    }
}
