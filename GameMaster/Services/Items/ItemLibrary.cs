namespace GameMaster.Services.Items;

/// <summary>
/// Placeholder class representing a collection of items available in the game.
/// The private repo loads item packs, metadata, and custom expansions.
/// </summary>
public class ItemLibrary
{
    public string[] ListAllItems()
    {
        // Real data structure stored only in the private repository.
        return System.Array.Empty<string>();
    }
}
